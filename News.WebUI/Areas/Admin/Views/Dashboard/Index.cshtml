@model List<Information>

<!--
Author: W3layouts
Author URL: http://w3layouts.com
-->
<!doctype html>
<html lang="zxx">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Traversal Travel Category Bootstrap Responsive Template | Destinations :: W3layouts</title>
    <!-- google fonts -->
    <link href="//fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link href="//fonts.googleapis.com/css2?family=Lato:ital,wght@0,300;0,400;0,700;1,400&display=swap"
          rel="stylesheet">
    <!-- google fonts -->
    <!-- Template CSS -->
    <link rel="stylesheet" href="/Traversal-Starter/assets/css/style-starter.css">
    <!-- Template CSS -->
</head>

<body>

    <!-- //about breadcrumb -->
    <!--  Work gallery section -->
    <section class="grids-1 py-5">
        <div class="grids py-lg-5 py-md-4">
            <div class="container">
                <div class="row ">

                    @foreach (var item in Model)
                    {
                        <div class="col-lg-4 col-md-4 col-6">
                            <div class="column">
                                <a href="/Home/GetByInformatioId/@item.InformationID"><img src="@item.PictureURL" style="width:370px;height:270px" alt="" class="img-fluid"></a>
                                <div class="info">
                                    <h2>@(item.Body.Length > 40 ? $"{item.Body.Substring(0, 40)}..." : item.Body)</h2>
                                    <p>
                                        <a href="/Home/GetByInformatioId/@item.InformationID">@(item.Body.Length > 100 ? $"{item.Body.Substring(0, 100)}..." : item.Body)</a>
                                    </p>
                                    <div class="dst-btm">
                                        <h5>@item.Created.ToString("dd MMM HH:mm") </h5>
                                        <h5>@item.Content.ContentName</h5>
                                        <br />
                                    </div>
                            
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <a id="delete" onclick="return confirm('Are you sure you want to delete this item?')" style="width:100%" href="/Admin/Dashboard/Delete/@item.InformationID" class="btn btn-danger">Delete</a>
                                    }
                                    <a style="width:100%" href="/Admin/Dashboard/Update/@item.InformationID" class="btn btn-warning">Update</a>                                 
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
    @if (User.IsInRole("Moderator"))
    {
        <a href="/Admin/Dashboard/Add" class="btn btn-info">Add</a>
    }
    <!--  //Work gallery section -->
    <!-- grids block 5 -->
    <!--/w3l-footer-29-main-->
    @* <footer>
    <!-- footer -->
    @await Component.InvokeAsync("Footer")
    <!-- //footer -->
    </footer>*@

    <!-- Template JavaScript -->
    <script src="/Traversal-Starter/assets/js/jquery-3.3.1.min.js"></script>
    <script src="/Traversal-Starter/assets/js/theme-change.js"></script>
    <!--/MENU-JS-->
    <script>
        $(window).on("scroll", function () {
            var scroll = $(window).scrollTop();

            if (scroll >= 80) {
                $("#site-header").addClass("nav-fixed");
            } else {
                $("#site-header").removeClass("nav-fixed");
            }
        });

        //Main navigation Active Class Add Remove
        $(".navbar-toggler").on("click", function () {
            $("header").toggleClass("active");
        });
        $(document).on("ready", function () {
            if ($(window).width() > 991) {
                $("header").removeClass("active");
            }
            $(window).on("resize", function () {
                if ($(window).width() > 991) {
                    $("header").removeClass("active");
                }
            });
        });
    </script>
    <!--//MENU-JS-->

    <script src="/Traversal-Starter/assets/js/bootstrap.min.js"></script>

</body>

</html>
































































@*@model List<Information>


<div class="container-fluid">
    <table id="tbl1" class="table table-light table-bordered table-hover" style="margin-top:20px">
        <thead>
            <tr>
                <th>Header</th>
                <th>Body</th>
                <th>Created</th>
                <th>Validated</th>
                <th>Content</th>
                @if (User.IsInRole("Admin"))
                {
                    <th>Delete</th>
                }
                <th>Update</th>
            </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td class="col-sm-6">@item.Header</td>
                <td>
                    @if (item.Body.Length <= 60)
                    {
                        @item.Body
                    }
                    else
                    {
                        @(item.Body.Substring(0, 60) + "...")
                    }
                </td>
                <td class="col-sm-3">@item.Created.ToString("d MMM yyyy")
                <td class="col-sm-6">@item.IsValid</td>
                <td class="col-sm-6">@item.Content.ContentName</td>
                    @if (User.IsInRole("Admin"))
                    {
                    <td><a id="delete" onclick="return confirm('Are you sure you want to delete this item?')" style="width:100%" href="/Admin/Dashboard/Delete/@item.InformationID" class="btn btn-danger">Delete</a></td>

                    }

                <td><a style="width:100%" href="/Admin/Dashboard/Update/@item.InformationID" class="btn btn-warning">Update</a></td>
            </tr>
        }
    </table>
</div>
@if (User.IsInRole("Moderator"))
{
    <a href="/Admin/Dashboard/Add" class="btn btn-info">Add</a>
}
*@

